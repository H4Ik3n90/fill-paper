// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

// model User {
//   id       String  @id @default(uuid())
//   username String  @unique
//   email    String  @unique
//   password String
//   Notes    Notes[]
// }

model Notes {
  id        String   @id @default(uuid())
  title     String
  body      String
  createdAt DateTime
  updateAt  DateTime
  // writter   User     @relation(fields: [userId], references: [id])
  userId    String
}
